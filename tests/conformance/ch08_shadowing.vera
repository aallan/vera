import vera.math(hundred);

public fn hundred(@Int -> @Int)
  requires(true)
  ensures(@Int.result == 0)
  effects(pure)
{
  0
}

public fn main(@Unit -> @Int)
  requires(true)
  ensures(@Int.result == 0)
  effects(pure)
{
  hundred(0)
}
