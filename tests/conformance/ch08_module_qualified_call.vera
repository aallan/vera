import vera.math;

public fn hundred(@Int -> @Int)
  requires(true)
  ensures(@Int.result == 0)
  effects(pure)
{
  0
}

public fn main(@Unit -> @Int)
  requires(true)
  ensures(@Int.result == 100)
  effects(pure)
{
  vera.math::hundred(0)
}
