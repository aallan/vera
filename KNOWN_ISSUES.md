# Known issues

Bugs and limitations tracked against the [issue tracker](https://github.com/aallan/vera/issues). This file is a curated snapshot — the issue tracker is the source of truth, and priority/sequencing live in [ROADMAP.md](ROADMAP.md). Every row follows the same shape: one sentence on what the issue is, one sentence on impact and the path forward.

## Bugs

Defects in shipped compiler, runtime, or tooling behaviour — this table matches the issue tracker's open [`bug`-labelled issues](https://github.com/aallan/vera/issues?q=is%3Aissue%20state%3Aopen%20label%3Abug) one-to-one. A verification-soundness defect (a proof or a clean verify that the running program contradicts, or a runtime check verify claims and code generation does not emit) carries the `soundness` label beside `bug`; verification incompleteness that fails closed, a proof left to a runtime check or a shape refused rather than miscompiled, carries `limitation` and is tracked under [Limitations](#limitations).

| Bug | Issue |
|-----|-------|
| `&&`, `\|\|` and `==>` evaluate both operands, although spec §4.6 says `&&` and `\|\|` short-circuit: a right operand that traps or performs an effect runs after the left one has decided the result, so `@Int.0 != 0 && 10 / @Int.0 > 0` traps on `0`, and the verifier, which matches the compiled code, takes no left operand as a premise of its right.  It also breaks §5.2.2's equivalence of several `requires` clauses with one joined by `&&`: write a precondition whose later conjunct relies on an earlier one as separate clauses, and guard an operation with a nested `if`. | [#1501](https://github.com/aallan/vera/issues/1501) |
| A value whose refinement the solver cannot STATE — a predicate calling something outside the decidable fragment, spec §2.6.4 cause 3 — is modelled with nothing said about it, so a refutation can rest on a value the type forbids.  `100 / @U.0` over <code>type U = { @Int &#124; int_to_float(@Int.0) &gt; 0.0 &amp;&amp; @Int.0 &gt; 0 }</code> is refused E526 with the divisor 0 at a parameter, a refined return and a type argument alike, on a program that runs and returns 25. | [#1470](https://github.com/aallan/vera/issues/1470) |
| A propositionally false precondition on a `forall` generic callee is disclosed at the call site (`call_pre`/`tier3`/**E532**, guard emitted, program accepted) where the same contract on a monomorphic callee is refuted (`call_pre`/`violated`/**E501**, program refused).  `requires(false)` has no model at any instantiation, so there is nothing for a runtime guard to check.  Distinct from [#1459](https://github.com/aallan/vera/issues/1459), which is the same call path recording its obligation TWICE: that is how many records are made, this is their strength.  Pre-existing at [`09d526bf`](https://github.com/aallan/vera/commit/09d526bf87a4), surfaced by [#1451](https://github.com/aallan/vera/issues/1451)'s claim that a call to an unsatisfiable-precondition function is refused — true monomorphically, false through a generic. | [#1468](https://github.com/aallan/vera/issues/1468) |
| A composite value coerced into a composite type with a `@Nat` component is obligated nowhere unless it is built at the site: `let @Array<Wrap<Nat>> = [W(@Int.0)]` verifies clean at -5 and traps at a later bind, and `let @Array<Wrap<Int>> = [W(@Nat.0)]` returns u64.MAX as -1. | [#1542](https://github.com/aallan/vera/issues/1542) |
| `let @Int = @Nat.0 + 1` at i64.MAX returns -2^63 with no obligation: arithmetic with a literal operand is never a genuine `@Nat`, so the u64 sum's widening is not recognised. | [#1543](https://github.com/aallan/vera/issues/1543) |
| A destructuring `let` is not checked against its source: `let Tuple<@Int, @String> = Tuple(1, 2)` passes check and verify and binds the integer 2 at `@String`. | [#1545](https://github.com/aallan/vera/issues/1545) |
| A heterogeneous `@Nat`/`@Int` join read into an `@Int` is guarded per arm only as a scalar `if` or `match`: built as a tuple component, or produced by a `handle`, u64.MAX comes back as -1. | [#1546](https://github.com/aallan/vera/issues/1546) |
| A `@Nat` subtraction over a call to a non-generic function or built-in declared to return `@Nat` is recorded `nat_sub` `tier3` and compiled with no underflow check: code generation reads such a call as `Int`.  `string_length(catch_text(())) - 16` in `ch07_exn_string_alias.vera` is claimed runtime-checked and unchecked, and `nat_id(@Nat.1) - @Nat.0` returns -3 from a `@Nat` function (verify refuses it with E502). | [#1557](https://github.com/aallan/vera/issues/1557) |
| `float_to_string` traps on a finite value of magnitude 2^63 or more (`1.0e19`, `-9.3e18`), although spec §9.6.11 makes it total over `Float64`: the finite path takes the integer part with `i64.trunc_f64_s`, which traps outside the i64 range.  Any program that renders a large float stops; the fix checks the magnitude before truncating. | [#1482](https://github.com/aallan/vera/issues/1482) |
| `string_repeat`, `string_pad_start` and `string_pad_end` narrow their `@Nat` size argument to 32 bits unchecked, so a count at or above 2^32 is reduced modulo 2^32: `string_repeat("ab", 4294967297)` returns `"ab"`.  The call returns a wrong value instead of refusing; the fix range-checks the size before narrowing. | [#1483](https://github.com/aallan/vera/issues/1483) |
| Four diagnostics about source code, the missing-visibility error on a `fn` or `data` declaration among them, carry no error code, so `--json` output has no `error_code` field and the text form no `[Exxx]` prefix, against spec §0.5.1.  Nothing enforces a code on every diagnostic; the fix assigns the codes and gates the registry so an uncoded diagnostic cannot be added. | [#1490](https://github.com/aallan/vera/issues/1490) |
| A user function named like a compiler-generated WebAssembly symbol (`alloc`, `gc_collect`, `anon_0`) passes check and verify, then fails at compile with a raw `duplicate func identifier` from WAT assembly, because user and generated functions share one symbol namespace.  Rename the function until every emitted symbol has exactly one owner. | [#1494](https://github.com/aallan/vera/issues/1494) |
| An entry-file function that takes a prelude function's name replaces it inside the prelude's own bodies: `json_get_string` then binds its `json_get` and `json_as_string` calls to the user's declaration, so the module fails to load (a different signature) or returns a different answer (the same signature).  Avoid prelude function names until prelude bodies resolve their calls in their own namespace. | [#1495](https://github.com/aallan/vera/issues/1495) |
| An entry-file `data` declaration that reuses a prelude ADT's name with a different shape passes check and verify, then fails at compile as soon as a built-in produces or consumes the type, because the checker types the built-in against the user's declaration.  Choose another name for the type until prelude ADT names are reserved uniformly ([#1469](https://github.com/aallan/vera/issues/1469)). | [#1496](https://github.com/aallan/vera/issues/1496) |
| `E608` refuses cross-module programs in which two same-named functions can never be named together (a selective import beside another module's own `f`, a module that shadows its import, two modules' private helpers of one name) after they pass check and verify, and its fix text asks for a rename inside a dependency.  These programs should compile once every emitted symbol has one owner. | [#1498](https://github.com/aallan/vera/issues/1498) |
| A call to a user ability's operation passes check and verify and never compiles: a bare call reports the function undefined, a call through a constrained generic reports `E613` with no location, and a qualified call such as `Eq.eq(1, 1)` fails with a raw WAT `unknown func`.  User-declared ability operations cannot be called at run time; the fix lowers them or refuses them at check. | [#1499](https://github.com/aallan/vera/issues/1499) |
| Spec §5.11 requires `main` to be `fn main(@Unit -> @Unit)` with `effects(<IO>)` or a superset, and the toolchain enforces neither part: a `main` returning `@Int` under `effects(pure)` passes check and verify, and `vera run` prints its value.  Four gated documentation examples have that shape; enforcement is a language change, made together with a VeraBench update. | [#1500](https://github.com/aallan/vera/issues/1500) |
| The host-implemented built-ins (regex, JSON, HTML, Markdown, Decimal) convert only the exceptions each one lists, so any other exception on an input the signature accepts ends the program as a `host_error` trap with no location and no Fix, and the `Result`-returning ones (`regex_*`, `json_parse`, `html_parse`, `md_parse`, `decimal_div`, `decimal_from_string`) trap instead of returning `Err`.  The fix converts every host exception into the signature's failure value. | [#1502](https://github.com/aallan/vera/issues/1502) |
| Code generation reads a `@Nat` with the signed `i64` instructions (`<`, `>`, `<=`, `>=`, `/`, `%` and every renderer), so a `@Nat` above i64.MAX is compared, divided and printed as a negative number, and a Tier-1 `ensures` fails at run time.  Keep values that can exceed i64.MAX out of `@Nat` comparisons, division and output until those operations use the unsigned instructions. | [#1504](https://github.com/aallan/vera/issues/1504) |
| `ast.format_type_expr` gives every type argument its own `@`, so diagnostics, obligation descriptions and runtime contract messages show `@Array<@Int>.0`, which does not parse (`E005` when copied back into source).  Only message text is wrong; the fix renders type arguments without the sigil. | [#1505](https://github.com/aallan/vera/issues/1505) |
| A quantifier's predicate signature is never checked, so a predicate code generation cannot lower (a refined parameter, a `Bool` parameter, an `Int` result, two parameters or none) passes check and verify, and compile stops with the internal error `E699`.  The fix refuses at check every predicate shape code generation cannot lower. | [#1506](https://github.com/aallan/vera/issues/1506) |
| The collector's mark worklist is a fixed 64 KiB (16,384 entries), so a garbage collection that runs while one live structure holds more than 16,384 heap values traps `unreachable` in `$gc_collect`.  An ordinary program holding a large array of heap values can die partway through; the fix grows the worklist or marks without one. | [#1510](https://github.com/aallan/vera/issues/1510) |
| A `State<String>` or `State<Array<T>>` cell (a type represented as a `(ptr, len)` pair) passes check and verify, and code generation skips every function that uses it with `E607`, though spec §7.3.3 illustrates a `State<String>` cell.  Cells of single-value types (`Int`, `Bool`, `Float64`, `Byte`) are unaffected; the fix lowers pair-typed cells. | [#1512](https://github.com/aallan/vera/issues/1512) |
| A `let` or match binder typed through a parameterised alias (`Id<Nat>` for `type Id<T> = T;`) passes check and verify, and code generation skips the function (`E602`, no WASM representation); passed on to a generic, the function can disappear with no diagnostic at all.  Parameters, results and lambda parameters of the same type compile; spell the binder's type without the alias. | [#1514](https://github.com/aallan/vera/issues/1514) |
| A generic called on a nested prelude-generic call, `idg(result_unwrap_or(Ok(3), 0))`, passes check and verify, and code generation drops the caller (`E602`): the call site names the clone `idg$Nat` while monomorphization discovery emits `idg$Int`.  The fix names both from one derivation. | [#1515](https://github.com/aallan/vera/issues/1515) |
| `apply_fn(mk(0), 41)`, where `mk` returns a closure, passes check and verify, and code generation refuses it (`E616`, then `E602`), though the closure's type is stated in `mk`'s signature.  Binding the closure with `let` first, typed through an alias, compiles. | [#1516](https://github.com/aallan/vera/issues/1516) |
| A result reference's type is never compared with the function's return type (spec §3.6), so on a function returning `@Int`, `ensures(@Qzt.result == 1)`, whose type nothing declares, and `ensures(@String.result == 1)` pass check and verify at Tier 1.  A mistyped `@T.result` is accepted silently; the fix refuses a head that is not the return type. | [#1517](https://github.com/aallan/vera/issues/1517) |
| A generic from a module that declares `type Shape = Int;`, called from a file at that file's own `data Shape`, passes check and verify, and code generation writes a module that fails to load (`type mismatch: expected i64, found i32`), because the substituted name reads as the module's alias inside the clone.  Avoid a caller type named like an alias in the generic's module until substituted data types have a capture-proof spelling. | [#1519](https://github.com/aallan/vera/issues/1519) |
| A generic `handle[State<T>]` nested in a `handle[State<Int>]`, with a `put` in the inner clause body, passes check and verify, and at `T = Int` code generation drops it (`E602`, then `E620`), where spec §7.5.2 says the same-family shape is refused at check (`E339`).  The check cannot see a family that forms only at instantiation; the fix is [#1233](https://github.com/aallan/vera/issues/1233)'s depth-indexed cell access, or a check-time refusal of the generic shape. | [#1522](https://github.com/aallan/vera/issues/1522) |
| A `String` thrown out of an entry point is quoted into the `uncaught_exception` message cut at 64 bytes, so a cut inside a multi-byte character ends the quoted value in U+FFFD.  Only the message text is affected; the fix cuts at a character boundary. | [#1526](https://github.com/aallan/vera/issues/1526) |
| `scripts/check_doc_counts.py` reads the CI job count from a `TESTING.md` sentence its pattern cannot match, and skips the check when nothing matches, so the job count is ungated (it has drifted: `TESTING.md` says nine jobs, `ci.yml` has ten).  A citation the gate cannot find should fail the gate, not pass it. | [#1527](https://github.com/aallan/vera/issues/1527) |
| A `decreases` clause the backend declines to guard (on a function whose effect row declares `Exn<T>`, or over a parameterized ADT measure such as `List<Int>`) compiles with no termination guard, yet `vera verify` records the obligation `tier3` and warns `E525` that it will be checked at runtime.  A non-decreasing recursion there runs unchecked while verify claims a runtime check; the fix emits the guard or stops claiming it. | [#1530](https://github.com/aallan/vera/issues/1530) |
| A `match` that misses a case only inside a nested constructor pattern (`W(Red)` with no `W(Green)` arm, or `Some(Red)` and `None` over `Option<Colour>`) passes the exhaustiveness check, and at run time the uncovered value takes the last arm.  The same gap one level up is `E311`; add a catch-all arm inside nested patterns until the check recurses. | [#1540](https://github.com/aallan/vera/issues/1540) |
| `show()` and `hash()` of a value whose type an imported function's signature names read that name through the importing file's type aliases, so an entry alias spelled like the imported type (`type Colour = String;`) captures the value and the program prints a wrong result.  Avoid entry aliases named like an imported module's types until the render resolves the name in its declaring module. | [#1548](https://github.com/aallan/vera/issues/1548) |
| Code generation discards a user function's `E602` when its line number falls inside a compiled generic template's span, without comparing files, so an entry-file function on a line inside a prelude generic's span is dropped with no diagnostic.  The run then reports only that there are no exported functions; the fix compares the file as well as the line. | [#1549](https://github.com/aallan/vera/issues/1549) |
| `show()` or `hash()` of a generic data type's value that a generic function returns is refused at compile (`E602`, `show() not supported for type 'Box'`): the clone is named after the argument type's bare head, so the result type reaches the render without its argument.  The fix keeps the clone's result type arguments. | [#1550](https://github.com/aallan/vera/issues/1550) |
| The verifier's monomorphization discovery resolves a module's call to its own `where` helper against the entry file's same-named generic, so per-monomorphization verification checks a clone code generation never emits and skips the one it does.  A contract on the emitted clone can go unchecked; the fix gives the verifier and code generation one discovery. | [#1551](https://github.com/aallan/vera/issues/1551) |
| A type alias whose body names an alias declared after it is registered with that name unresolved, so every use of the first alias is refused (`E140`, `E170`, `E202`) with errors that do not point at the alias.  Declare the named alias first. | [#1552](https://github.com/aallan/vera/issues/1552) |
| A `type` alias named `Decimal` or `Array`, used beside a built-in value of that type, builds a module that fails to load; `data` declarations of those names are refused with `E158`, and aliases are not.  Choose another alias name until alias names are reserved as well. | [#1553](https://github.com/aallan/vera/issues/1553) |
| `==` between two call results of a generic data type compiles to a heap-address comparison (`i32.eq`), so `mo(7) == mo(7)` over `Option<Int>` is `false`, while the verifier models `==` structurally and proves a Tier-1 `ensures` that fails at run time.  A soundness gap between the verifier and code generation; the fix emits the structural comparison for call-result operands. | [#1555](https://github.com/aallan/vera/issues/1555) |
| A function whose type parameter is named `Array` passes check and verify, and when it is called with an array, code generation drops the caller (`E602`, `idd$Array` not registered); the `E158` reservation covers `data` names only.  Rename the type parameter. | [#1556](https://github.com/aallan/vera/issues/1556) |
| A module that declares its own `data Json` accepts a prelude `Json` value (a `json_parse` result) where its own type is expected, because data types are identified by bare name, and reads it through its own layout: a JSON number prints as the bit pattern of the float.  Do not declare a module-local `Json` until the prelude type has its own owner. | [#1560](https://github.com/aallan/vera/issues/1560) |
| The verifier decides which `match` arm runs from each pattern's outer constructor only, so `Some(None)` is taken to match every `Some`, a later `Some(Some(@Int))` arm is treated as unreachable, and an `ensures` is proved at Tier 1 that fails at run time.  A soundness gap; the fix models nested sub-patterns in `_pattern_condition` (`vera/smt.py`). | [#1562](https://github.com/aallan/vera/issues/1562) |
| When a refined binder's refinement cannot be proved (`E505` at a refined `let`, `E503` at an `@Nat` arm binder), a later call whose precondition that refinement satisfies is refused again with `E501`.  The second error is a consequence of the first and goes away when the binder is fixed. | [#1563](https://github.com/aallan/vera/issues/1563) |
| Indexing an array literal directly (`[true][0]`) passes check and verify, and code generation drops the function (`E602`, could not infer the index expression's element type).  Bind the array with `let` first. | [#1564](https://github.com/aallan/vera/issues/1564) |
| A call inside a callee's `ensures` has its precondition checked at every caller, where the verifier cannot state the argument, so a `main` calling `f(5)` is refused `E501` for a call only `f`'s postcondition makes.  A correct program is rejected; the fix checks that precondition once, in the callee. | [#1567](https://github.com/aallan/vera/issues/1567) |
| A call whose precondition fails raises no `call_pre` obligation when it sits in a `match` arm, a closure body, a handler clause or a string interpolation, so `vera verify` passes and `vera run` traps on the precondition.  The same call in a function body, a `let`, an `if` branch or an argument is `E501`; the fix obligates the call at every position. | [#1570](https://github.com/aallan/vera/issues/1570) |
| A callee's `ensures` is assumed under path conditions that over-approximate where the call runs (an `if` whose condition does not translate, or a `match` arm an earlier arm shadows), so a postcondition is proved at Tier 1 from a call that never happens and fails at run time.  A soundness gap in the path conditions `_guard_fact` asserts under. | [#1571](https://github.com/aallan/vera/issues/1571) |
| When a module calls another module's generic by its qualified name, the verifier never checks that instantiation, though code generation emits it, so a generic whose `ensures` is false passes `vera verify` and everything proved from it reports Tier 1.  A soundness gap; the bare-call form is verified. | [#1572](https://github.com/aallan/vera/issues/1572) |
| The `@Nat`-subtraction trap message drops the parentheses around a compound right operand, so `@Nat.1 - (@Nat.0 - 1)` is reported as `@Nat.1 - @Nat.0 - 1`, a different expression.  Only the message text is wrong; the suggested `requires` in the same message is correct. | [#1573](https://github.com/aallan/vera/issues/1573) |
| A module generic imported by the entry, reached only through another generic's instantiation, whose body calls a private generic sibling, passes check, and code generation never emits the sibling's instantiation, so `main` is dropped (`E602`, then `E620`).  Calling the generic directly from the entry compiles. | [#1575](https://github.com/aallan/vera/issues/1575) |
| A `match` whose scrutinee is an `array_fold` call or a pipe passes check and verify, and code generation drops the function (`E602`, could not infer the scrutinee's WASM type).  Bind the value with `let` first. | [#1578](https://github.com/aallan/vera/issues/1578) |
| A pipe whose callee returns `@Nat`, read into an `@Int`, is never treated as a widening, so it gets no obligation and no guard, and u64.MAX comes back as -1.  The same value through a plain call is obligated and guarded; call the function directly until pipes are classified the same way. | [#1580](https://github.com/aallan/vera/issues/1580) |
| In a file that imports the module, a module generic's call to its own `where` helper compiles as a call to the module's same-named top-level generic, while the verifier reads it as the helper, so an `ensures` proved at Tier 1 fails at run time.  A soundness gap; give the helper a name no top-level function in its module uses. | [#1581](https://github.com/aallan/vera/issues/1581) |
| A `@Nat`-to-`@Int` widening inside an `assume` is recorded `nat_to_int_coerce` `tier3` and counted in `tier3_runtime`, though an `assume` never runs and no guard exists for it.  The verification summary overstates its runtime checks; the fix records no runtime check for an assumed expression. | [#1582](https://github.com/aallan/vera/issues/1582) |
| An array literal takes its element type from its first element and never checks the others (spec §4.12.1 requires one type), so a mixed-type literal passes check and fails at compile or load, and inside a generic body passes a literal as a rigid `T`.  The fix checks every element against the element type. | [#1584](https://github.com/aallan/vera/issues/1584) |
| A `match` over a join whose arms build the same constructor guards the join's `@Nat` argument for an `@Int` binder in a later arm even when an earlier arm takes the value, so a valid program traps at a `@Nat` above i64.MAX.  It fails closed: a trap, never a wrong value. | [#1586](https://github.com/aallan/vera/issues/1586) |
| A literal-only expression below `Int`'s minimum, `0 - 18446744073709551615`, is verified as that value and compiled as its 64-bit wrap, so `ensures(@Int.result < 0)` is proved at Tier 1 and the run returns 1.  A soundness gap; the fix refuses or obligates a literal-only value outside `Int`'s range. | [#1587](https://github.com/aallan/vera/issues/1587) |
| Binding a `@Nat` above i64.MAX out of a tuple or constructor pattern into a `@Nat` binder traps as a negative value, though the value and the binder are both `@Nat`.  It fails closed, and `vera verify` is clean. | [#1589](https://github.com/aallan/vera/issues/1589) |
| `nat_to_int` of a `@Nat` above i64.MAX returns its bits as a negative `@Int`, unguarded, wherever the result is not bound into an `@Int` slot (as an arithmetic or comparison operand), while the verifier models it as the true value, so a Tier-1 `ensures` fails at run time.  Binding the result into an `@Int` `let` is guarded. | [#1590](https://github.com/aallan/vera/issues/1590) |
| A `@Nat` sum or product holding a negative literal-only part (`@Nat.0 + (if b then { 0 - 3 } else { 0 })`), read by an `@Int` operation, is neither obligated nor guarded, so a `@Nat` above i64.MAX inside it is read as negative and the result is wrong.  The checker types the `if` at its `else` branch's `@Nat`; the fix types literal-only branches from their context. | [#1591](https://github.com/aallan/vera/issues/1591) |
| A unary negation of a `@Nat` is an `@Int` operation on the `@Nat`'s bits with no check, so `-@Nat.0` at 2^63 + 5 returns 9223372036854775803 and a Tier-1 `ensures` fails at run time.  A soundness gap; the fix obligates and guards the operand's widening. | [#1592](https://github.com/aallan/vera/issues/1592) |
| A `@Nat` added to an `if` over literals with a negative branch runs at the unsigned width, because the checker types the `if` at its `else` branch's `@Nat`: `@Nat.0 + (if b then { 0 - 1 } else { 0 })` at 5 traps with a false overflow instead of returning 4.  It fails closed; the fix types literal-only branches from their context. | [#1593](https://github.com/aallan/vera/issues/1593) |
| A `Some(Tuple(...))` built from `@Nat` values and matched back out as `Tuple<Int, Int>` crashes the verifier with a Z3 sort mismatch, reported as the internal error `E699`.  `vera verify` gives no verdict for the function; a payload built from literals verifies. | [#1594](https://github.com/aallan/vera/issues/1594) |
| A recursive call whose argument is an untranslatable call leaves both its `decreases` (`E525`) and its precondition (`E532`) at Tier 3, even when the measure does not read that argument.  Both are still checked at run time; binding the call's result with `let` first, as `examples/life.vera` does, keeps both proofs at Tier 1. | [#1595](https://github.com/aallan/vera/issues/1595) |
| A negative literal inside an arithmetic operand of a generic call bound to `@Nat`, `let @Nat = id(0 - 3) + 1`, passes `vera verify`, and only the runtime guard catches the negative value (`Negative value bound into a @Nat slot`).  It fails closed, a trap and never a wrong value; the `let`'s narrowing of the sum should be refused statically (`E503`). | [#1596](https://github.com/aallan/vera/issues/1596) |
| A `handle[E]` for an effect other than `State<T>` or `Exn<E>` (`handle[Http]`, `handle[IO]`, a user-declared effect) passes check and verify, and code generation drops the enclosing function (`E602`).  A host effect cannot be mocked in Vera code yet ([#372](https://github.com/aallan/vera/issues/372)); until such handlers compile, the shape should be refused at check. | [#1597](https://github.com/aallan/vera/issues/1597) |
| `@Int` division carries only a divide-by-zero obligation, so `INT_MIN / -1`, on which WebAssembly's `i64.div_s` traps, has none: `examples/safe_divide.vera` verifies at Tier 1 and traps with `Integer overflow` on `-9223372036854775808 / -1`.  A soundness gap that fails closed (a trap, never a wrong value); the fix obligates the overflow case. | [#1598](https://github.com/aallan/vera/issues/1598) |
| A pipe written as a tuple's or a constructor's component (`Tuple(1, 3 \|> id())`, `MkBox(3 \|> id())`) passes check and verify, and code generation drops the function (`E602`, could not infer the constructor argument's WASM type).  Bind the piped value with `let` first. | [#1599](https://github.com/aallan/vera/issues/1599) |
| An entry file that imports a module's own `data Json` resolves the type name `@Json` to the prelude's `Json`, so a `match` over the module's constructors is refused as non-exhaustive (`E311`) for the prelude's.  Name the module's type differently from the prelude's (`Jsn`) until an imported name takes precedence over the prelude's. | [#1600](https://github.com/aallan/vera/issues/1600) |
| An `assume` over a `let` bound to a user function's result constrains a different term from the one the body returns, so an `ensures` it should establish is refused with `E500`.  It fails closed; an `assume` over a parameter or over arithmetic reaches the `ensures`. | [#1601](https://github.com/aallan/vera/issues/1601) |
| The explanation E506 prints for an unguarded refinement lists construction positions (a constructor field, a tuple component, an array element, a `Map` value) among the sites code generation does not guard, which they are not since [#1426](https://github.com/aallan/vera/issues/1426).  Only the message text is wrong; the fix lists the sites codegen leaves unguarded today. | [#1602](https://github.com/aallan/vera/issues/1602) |
| Code-generation codes E603, E604, E605 and E607 carry registry titles that describe something else (E603 "Unsupported closure" is emitted for an unsupported effect), and E604 and E605 duplicate E600 and E601 for an unsupported parameter and return type.  A lookup in `vera errors` names the wrong cause; the fix gives each concept one code and a title that matches it. | [#1603](https://github.com/aallan/vera/issues/1603) |
| Indexing the result of a pipe, `(@Array<Int>.0 \|> array_reverse())[0]`, passes check and verify, and code generation drops the function (`E602`, could not infer the index expression's element type).  Write the call form, `array_reverse(@Array<Int>.0)[0]`, or bind the piped value with `let` first. | [#1604](https://github.com/aallan/vera/issues/1604) |
| A `Nat` collection context (`@Array<Nat>`, `@Set<Nat>`, `@Map<_, Nat>`) refuses a call that returns only the non-negative elements of a collection built beside a negative literal: `let @Array<Nat> = array_slice([-3, 5], 1, 2)` is refused with `E503` and traps, where `main` returns `[5]`.  The refusal keeps a collection that does hold the negative from binding unguarded; it lifts once the `Array<Int>` to `Array<Nat>` coercion is obligated element by element ([#1542](https://github.com/aallan/vera/issues/1542)).  Bind the call at `@Array<Int>` until then. | [#1605](https://github.com/aallan/vera/issues/1605) |
| A `requires` or `ensures` that code generation cannot translate, such as a quantified `ensures`, is compiled with no runtime check, yet `vera verify` records it `tier3` (`E523`, "Contract will be checked at runtime") and counts it in `tier3_runtime`, so a run can return a value the contract forbids.  The fix emits the check or stops claiming it; until then, state such a contract in a form code generation can express. | [#1607](https://github.com/aallan/vera/issues/1607) |
| A `map_insert` value stored where no `let` or return names the map's type — a call argument, a `Some` payload, a user constructor field — is guarded at the store but never obligated: `h(map_insert(map_new(), "k", float_to_int(@Float64.0)))` into a `@Map<String, Nat>` parameter traps on a negative value while `vera verify` records no `nat_bind`, so the account omits a check the module holds and a statically refutable value is not refused.  The verifier enters its construction descent for a `map_insert` only from a `let`'s or a return's declared type; bind the map with a typed `let` first to get the obligation. | [#1608](https://github.com/aallan/vera/issues/1608) |
| An integer-literal `match` over a call that returns `@Byte` passes check and verify, and code generation drops the function (`E602`, "IntPattern over a scrutinee of type Bool"): the scrutinee's type is recovered from a declared return only when it has type arguments, so a bare `@Byte` falls to the i32 fallback, which names it `Bool`.  Bind the call with `let @Byte = …` and match the slot. | [#1609](https://github.com/aallan/vera/issues/1609) |
| `vera/proposeEdit` refuses an edit that introduces an obligation with status `tier3` but applies one that introduces an obligation with status `timeout`: `proof_delta` files a timeout under `timed_out`, which no conjunct of the gate reads, and a timeout is a warning, so the diagnostics count does not see it either. | [#1610](https://github.com/aallan/vera/issues/1610) |

## Limitations

Things Vera cannot do yet, as distinct from defects in what it claims to do.

| Limitation | Issue |
|-----------|-------|
| Nested handlers over the SAME `State<T>` cannot honour spec §7.5.2's enclosing-context rule for a clause-body bare `get`/`put`. The clause TRANSFORM half routes outward correctly, but the host cell intrinsics (`state_get_T` / `state_put_T`) address only the innermost pushed cell of a family, so the operation would read or write the INNER cell where the rule names an outer one — including at a distance, as in an `Int`/`Nat`/`Int`/`Nat` nest whose third level routes to the second. The shape is refused at check time (`E339`) where the operation is written, rather than lowered to hybrid semantics — except in a generic whose inner cell is `State<T>`, which becomes the same family only at an instantiation, so the program passes check and code generation drops that instantiation (`E602`, [#1522](https://github.com/aallan/vera/issues/1522)); the fix is depth-indexed host access (`state_get_at`/`state_put_at` or push-time cell handles) across the native, browser and WASI hosts, plus the verifier's obligation keying for depth-addressed cells. Different-family nesting is unaffected. Outward clause re-entry is separately capped at 8 levels (`STATE_CLAUSE_INLINE_DEPTH_CAP`), because each re-entry re-expands another clause body and the emitted code is exponential in the nesting depth; a deeper chain is refused at check time too (`E339`). | [#1233](https://github.com/aallan/vera/issues/1233) |
| An ADT `decreases` measure is runtime-ranked only when its type's reachable field structure is fully concrete: a parameterized measure (`List<Int>`), or a concrete type whose recursion rides through a parameterized field, gets no runtime guard (never a wrong one — the registered generic layout does not describe concrete construction), staying statically checked and Tier-3-disclosed. Per-instantiation `$dec_size_<T>` helpers (the `$eq_<type>` pattern) close it. | [#1177](https://github.com/aallan/vera/issues/1177) |
| Tier 2 verification (Z3 guided by `assert`/lemma hints) is specified in §6.3.2 but not implemented, so contracts that need hints fall to Tier 3 runtime checks. Per-monomorphization verification (#732) has since landed; full Tier 2 (hint-guided) stays on the Milestone 4 horizon. | [#427](https://github.com/aallan/vera/issues/427) |
| `data invariant(...)` clauses (spec §2.6, §6.2.3) are not implemented — every documented form fails with E130 because the slot environment for the invariant predicate isn't wired up. Refinement types (`{ @T \| predicate }`, §2.6) are the working alternative until this lands. | [#686](https://github.com/aallan/vera/issues/686) |
| Contracts that depend on a `handle[...]` expression's value fall to Tier 3 — the verifier does not model the `handle[...]` expression's value or handler-clause state, so an `ensures` over a handle result is runtime-checked rather than statically proved. (Primitive-op and binding obligations *inside* the handle body walk at enclosing-scope precision and can prove Tier-1; a clause body's slot-dependent obligations are fresh-scope Tier-3, while a manifest violation there — `5 / 0` — stays a loud compile error.) | [#439](https://github.com/aallan/vera/issues/439) |
| `IO.read_line` / `IO.read_char` block indefinitely — there is no non-blocking or timed stdin read, so a program cannot wait on a key press and a clock at once (input concurrency). | [#770](https://github.com/aallan/vera/issues/770) |
| A value's postcondition / refinement is not propagated **through an ADT field**: storing a provably-constrained value (e.g. a `mag(@Int -> @Int) ensures(@Int.result >= 0)` result) into a `data` constructor field and recovering it with `match` forgets the constraint, so a postcondition whose truth depends on it cannot be discharged at Tier 1 — it degrades to a Tier-3 runtime check (or, on some shapes, is reported as an unprovable obligation). Conservative incompleteness, not unsoundness: the false variant is still correctly rejected and the contract is enforced at runtime; identical for same-file and cross-module callees. Same reporting/completeness family as the (since-closed) #764 and #779. | [#909](https://github.com/aallan/vera/issues/909) |
| `vera test` cannot generate inputs for functions with ADT parameters, so those functions are skipped with a warning. Constructor synthesis with recursive field generation is the planned fix (Milestone 1). | [#440](https://github.com/aallan/vera/issues/440) |
| `vera test`'s input generator builds its `SmtContext` without the function lookup, ADT registry, or recorded-type hook the verifier has, so a `requires` that calls a user function — or any built-in without an explicit translation branch — is untranslatable there and the target is skipped, though the verifier translates the same contract fine. Every such skip is disclosed by name; threading the function lookup and callee-scope machinery through recovers the coverage. | [#1249](https://github.com/aallan/vera/issues/1249) |
| Effect row variables cannot be unified, so higher-order functions polymorphic over their effect rows are not expressible. Full effect polymorphism is Milestone 4 work. | [#294](https://github.com/aallan/vera/issues/294) |
| Every `vera` invocation re-parses and re-checks the whole module graph from scratch. Incremental compilation is Milestone 4 work; the LSP server's warm `VerificationSession` already covers the editor loop. | [#56](https://github.com/aallan/vera/issues/56) |
| A module cannot re-export an imported symbol, so deep module trees force consumers to import from the defining file. Milestone 4 work. | [#127](https://github.com/aallan/vera/issues/127) |
| There is no package system or registry — all code shares one module tree resolved from the filesystem. Milestone 4 work; the issue carries the design discussion. | [#130](https://github.com/aallan/vera/issues/130) |
| There is no interactive read-eval-print loop; the shortest feedback path is `vera run` on a file. Milestone 3 developer-experience work. | [#224](https://github.com/aallan/vera/issues/224) |
| The language server resolves module imports from disk rather than open editor buffers, so unsaved changes in imported files aren't seen. Buffer-aware resolution is roadmap Tier 3. | [#724](https://github.com/aallan/vera/issues/724) |
| `vera lsp` will not run on Python 3.16+: its `pygls` dependency calls `asyncio.iscoroutinefunction`, removed in 3.16. Out of Vera's hands until pygls migrates to `inspect.iscoroutinefunction`; tracked so the `[lsp]` extra's support window is explicit (the rest of the toolchain is unaffected). | [#753](https://github.com/aallan/vera/issues/753) |
| LSP slot go-to-definition covers parameters only — `let` and `match` bindings aren't navigable, and there is no mechanical slot-index rewriting on signature change. Roadmap Tier 3. | [#181](https://github.com/aallan/vera/issues/181) |
| There is no date or time handling beyond `IO.time` — no ISO 8601 parsing, formatting, or arithmetic. Milestone 2 server-adjacent work. | [#233](https://github.com/aallan/vera/issues/233) |
| There are no cryptographic primitives — no hashing, no HMAC. Milestone 2 work, needed for API-authentication patterns like webhook signatures. | [#235](https://github.com/aallan/vera/issues/235) |
| There is no CSV parsing or generation. Milestone 2 server-adjacent work; JSON is the workaround interchange format today. | [#236](https://github.com/aallan/vera/issues/236) |
| The experimental `--target wasi-p2` (v0.0.194) routes IO, clocks, and random through standard component interfaces and `--world server` (v0.0.195) serves `wasi:http`, but the surface is IO + Random only — Http and every other host family are rejected under wasi-p2 with a diagnostic, and the default `wasm` target still uses ad-hoc `vera.*` host imports. Extending the wasi-p2 target to `wasi:http` outbound, streaming filesystem, and sockets is Milestone 2 server-effect work. | [#853](https://github.com/aallan/vera/issues/853) |
| WASM execution has no configurable fuel, memory, or timeout limits, so pathological computation runs unbounded. Milestone 2 work, essential for server workloads on untrusted input. | [#239](https://github.com/aallan/vera/issues/239) |
| `Http.get`/`Http.post` send fixed headers — callers cannot add custom ones such as `Authorization`. Milestone 2 Http-hardening work. | [#351](https://github.com/aallan/vera/issues/351) |
| Http responses surface only the body — status codes are not accessible, so callers cannot distinguish a 404 from a 500. Milestone 2 Http-hardening work. | [#352](https://github.com/aallan/vera/issues/352) |
| Http requests have no per-request timeout control. Milestone 2 Http-hardening work; today a hung server hangs the program. | [#353](https://github.com/aallan/vera/issues/353) |
| The browser runtime implements Http via deprecated synchronous XMLHttpRequest, which browsers throttle. Milestone 2 Http-hardening work, currently blocked (see the issue's relationships). | [#355](https://github.com/aallan/vera/issues/355) |
| Http supports GET and POST only — no PUT, PATCH, or DELETE. Milestone 2 Http-hardening work. | [#356](https://github.com/aallan/vera/issues/356) |
| `Inference.complete` hardcodes `max_tokens` and `temperature`. Configurability is Milestone 2's first Inference-hardening item — agent workloads need both for cost gates and deterministic replays. | [#370](https://github.com/aallan/vera/issues/370) |
| `Inference.embed` (vector embeddings) is not implemented. Blocked on the float-array host-alloc infrastructure (#373). | [#371](https://github.com/aallan/vera/issues/371) |
| The Inference effect cannot be handled in user code — `handle[Inference]` is rejected. Full handler support enables mocking, caching, and routing strategies (Milestone 2). | [#372](https://github.com/aallan/vera/issues/372) |
| Host imports cannot return `Array<Float64>` — the `alloc_result_ok_float_array` infrastructure doesn't exist. Required by `Inference.embed` (#371). | [#373](https://github.com/aallan/vera/issues/373) |
| The `<DB>` effect is Phase 1: SQLite-only, a single connection per run, and positional stringly-typed rows (`Array<Array<Option<String>>>`). Named columns, typed cells, additional backends, and transactions are later phases. (`handle[DB]` is separately blocked on host-effect handlers, #372.) | [#1143](https://github.com/aallan/vera/issues/1143) |
| `runtime.mjs` doesn't export its string-marshalling helpers, so JavaScript cannot pass `String` arguments into Vera functions in the browser. This forces browser programs into the compute-upfront/drain-stdout pattern; exporting the helpers is roadmap Tier 3. | [#603](https://github.com/aallan/vera/issues/603) |
| `IO.sleep` busy-waits the browser's main thread, freezing the tab for the sleep duration — animations and paced simulations don't run meaningfully under `--target browser`. The JSPI-based suspend/resume fix is roadmap Tier 3, demoted below correctness work. | [#609](https://github.com/aallan/vera/issues/609) |
| ANSI escape sequences render as literal control characters in the browser DOM, so terminal-style programs display garbage under `--target browser`. A minimal ANSI-subset interpreter in `runtime.mjs` is roadmap Tier 3. | [#610](https://github.com/aallan/vera/issues/610) |
| A refinement whose base is itself a refinement (<code>type Small = { @Pos &#124; @Pos.0 &lt; 10 }</code>) passes check and is proved at Tier 1, and code generation refuses any function whose signature names it with `E618`, because the boundary guard lowers one refinement level and would drop the inner predicate.  A program using the shape cannot run; spell the refinement flat over its primitive base (<code>{ @Int &#124; @Int.0 &gt; 0 &amp;&amp; @Int.0 &lt; 10 }</code>), which compiles and is guarded, until the guard composes the whole chain. | [#1450](https://github.com/aallan/vera/issues/1450) |
| A module generic whose `decreases` measure is one of the module's data types is proved at Tier 1 in the module's own run and falls to `tier3` (`E525`) through an importer, where the clone's measure translates to an `Int` term.  It fails closed: the measure is still checked at run time. | [#1577](https://github.com/aallan/vera/issues/1577) |
| A generic `where` helper on a recursion cycle with its generic parent leaves its own `decreases` at `tier3` (`E525`), though every call on the cycle decreases and the parent's is proved.  It fails closed: the measure is checked at run time. | [#1585](https://github.com/aallan/vera/issues/1585) |

## Refactoring needed

No files currently need decomposition.

## Test coverage gaps

Internal test-quality items that don't affect correctness today but would make the suite more durable to refactoring.

| Gap | Issue |
|-----|-------|
| Five test cells can pass without exercising what they assert: a provenance scan whose reduction hides an unread branch, a compile inside the `try` that counts a compile error as "did not run", a run check that accepts a trap's line number, a fixture that does not parse, and a duplicated assertion. | [#1611](https://github.com/aallan/vera/issues/1611) |
| Stale comments and docstrings that misstate the code beside them, a test cache that never hits, and a verifier built with its arguments in the wrong positions. | [#1612](https://github.com/aallan/vera/issues/1612) |

## CI workarounds

Defensive measures currently applied in CI (CVE ignores, forced upgrades, etc.) with explicit removal triggers — bridges, not permanent exceptions.

| Workaround | Where | Rationale | Remove when | Issue |
|------------|-------|-----------|-------------|-------|
| `codecov/codecov-action` SHA-pinned to a commit (`e79a696` = v6.0.1) instead of the floating `@v6` tag | Both Codecov upload steps in `.github/workflows/ci.yml` | Codecov was acquired by Harness (June 2026), elevating the risk that the floating tag is repointed and silently flows into CI; pinning to a reviewed commit closes that. Coverage enforcement is unaffected — the 80% gate is the on-runner `--cov-fail-under`, and uploads are `fail_ci_if_error: false`. | The Codecov → Harness migration stabilises — then re-evaluate, including whether to adopt a repo-wide SHA-pin policy. | [#712](https://github.com/aallan/vera/issues/712) |

## Runtime workarounds

Defensive measures applied in compiler / runtime code that exist to bridge an upstream-fixed-but-unreleased bug — same shape as the CI workarounds table, but the workaround sits in shipped code rather than CI config.

No runtime workarounds currently in place.
